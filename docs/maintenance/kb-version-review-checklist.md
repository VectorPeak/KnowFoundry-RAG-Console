# Knowledge Base Version Review Checklist

Before activating a candidate knowledge-base version:

- Confirm the candidate version was built from the intended scenario files.
- Run ingestion quality checks and keep the report path.
- Compare retrieval results against the current active version.
- Run a small regression set before activation.
- Keep rollback instructions visible in the release notes.

This review keeps version activation auditable and reversible.
